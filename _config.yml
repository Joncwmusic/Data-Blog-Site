title: Not Your Employer's Data Blog
remote_theme: "pages-themes/cayman@v0.2.0"
defaults:
  - scope:
      path: some-path
      layout: some_layout
    values:
      strip_title: true
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
description: A collection of projects and tutorials
